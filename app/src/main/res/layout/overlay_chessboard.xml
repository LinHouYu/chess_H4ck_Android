<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Drag Header Handle -->
        <LinearLayout
            android:id="@+id/dragHeader"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:background="@drawable/bg_drag_header"
            android:gravity="center"
            android:orientation="horizontal">

            <View
                android:layout_width="48dp"
                android:layout_height="4dp"
                android:background="#88FFFFFF" />
        </LinearLayout>

        <!-- Chessboard Canvas -->
        <com.linhouyu.chess_h4ck.ui.overlay.ChessboardView
            android:id="@+id/chessboardView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

    <!-- Resize Grip at Bottom-Right Corner -->
    <ImageView
        android:id="@+id/resizeGrip"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="bottom|end"
        android:padding="6dp"
        android:src="@drawable/ic_resize_grip" />
</FrameLayout>
